# -*- mode: snippet -*-
#name: impl Display for Type { fn fmt (...) }
#key : display
#contributor : Mixed contributors
# --
impl std::fmt::Display for ${1:Name} {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "$0")
    }
}
